import hdfsPickerConfig from 'ui/utils/hdfs-picker-config';
import { module, test } from 'qunit';

module('Unit | Utility | hdfs picker config');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hdfsPickerConfig();
  assert.ok(result);
});
